name: Deploy GitHub Pages
on:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Do not publish Fluidstack to GitHub Pages."
          echo "davidtphung.github.io 301s to the Sere user custom domain."
          echo "Use https://fluidstack-web.vercel.app/site-fit/ instead."
          exit 1
